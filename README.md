# Batch-Auction-Simulator
A discrete-time call auction simulator in pure Python. Enter BUY/SELL orders, it finds the single clearing price that maximizes matched volume, then prints trades. Supports multiple symbols, pro-rata fills, and reading/writing CSV sessions.
